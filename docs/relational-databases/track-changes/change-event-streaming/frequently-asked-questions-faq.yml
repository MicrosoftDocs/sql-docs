### YamlMime:FAQ
metadata:
  title: "Frequently Asked Questions (FAQ) for Change Event Streaming"
  description: "Find answers to common questions about the change event streaming feature for [!INCLUDE [sssql25-md](../../../includes/sssql25-md.md)]."
  ms.date: 07/29/2026
  ms.service: sql
  ms.topic: concept-article
  ai-usage: ai-assisted
  author: nzagorac-ms
  ms.author: nzagorac
  ms.reviewer: mathoma
  monikerRange: " = sql-server-ver17 || = sql-server-linux-ver17 "    
  ms.custom: build-2025
title: Frequently Asked Questions (FAQ) for Change Event Streaming (CES)
summary: |
  [!INCLUDE [sqlserver2025](../../../includes/applies-to-version/sqlserver2025-asdb-asmi.md)]
  The following are answers to questions about the Change Event Streaming (CES) feature for [!INCLUDE [sssql25-md](../../../includes/sssql25-md.md)], Azure SQL Database, and Azure SQL Managed Instance.

  [!INCLUDE [change-event-streaming-preview](../../../includes/change-event-streaming-preview.md)]

sections:
  - name: Compatibility and requirements
    questions:
      - question: Does CES work with Azure SQL Database?
        answer: Yes. CES is supported in Azure SQL Database starting in November 2025.
      - question: Is the full recovery model required for CES?
        answer: Yes. CES relies on reading the transaction log, so the SQL Server database must be configured with the full recovery model.
      - question: What is the cost of using CES?
        answer: For CES, Azure Event Hubs usage is billed at standard rates. Ingress and egress charges also apply.

  - name: Performance and impact
    questions:
      - question: What is the expected performance impact on a large database with millions of rows when CES is enabled?
        answer: The performance impact depends on your specific workload and configuration. However, CES is expected to have a lower overhead than change data capture (CDC), as there is no writing data back into the database.
      - question: What is the maximum message size supported by CES?
        answer: |
          CES uses the `@max_message_size_kb` setting to split large outbound events into message chunks that fit the destination [Azure Event Hubs limits](/azure/event-hubs/event-hubs-quotas). This setting controls message splitting only. It doesn't change the per-column truncation limit: before CES forms the outbound event, each streamed column value larger than 1 MB is truncated to 1 MB.
      - question: Will the transaction log continue to grow if CES can't deliver events (such as due to a destination issue, expired authorization credential, etc.)?
        answer: Yes. CES ensures reliable ("at least once") message delivery, so the transaction log won't be truncated until events are successfully delivered. If delivery fails, the log grows until the issue is resolved. It's important to maintain the health of CES by monitoring message delivery, [errors](#how-can-i-monitor-ces-activity--for-example--events-processed--errors-), and the utilization of the transaction log for databases that have CES enabled. 

  - name: Schema and data handling
    questions:
      - question: How does CES handle schema changes, such as deleting a column?
        answer: CES includes the row schema with each event. If a DDL operation modifies the table (such as deleting, renaming, or adding a column), the next DML event reflects the updated schema. CES doesn't emit events for DDL operations themselves.
      - question: Are tables with LOB columns (for example, varchar(max)) supported by CES?
        answer: Yes. CES supports tables with LOB columns. For each table configured for streaming, you can control whether the **old** row includes LOB column values by using [sys.sp_add_object_to_event_stream_group](../../system-stored-procedures/sys-sp-add-object-to-event-stream-group-transact-sql.md). Each streamed LOB value is still subject to the 1 MB per-column truncation limit. Configure the [max text repl size](../../../database-engine/configure-windows/configure-the-max-text-repl-size-server-configuration-option.md) server configuration option to allow more than 65,536 bytes to be written to LOB columns when CES is enabled.
      - question: What happens if a streamed column value is larger than 1 MB?
        answer: |
          If one or more streamed column values are larger than 1 MB, CES truncates each affected column value to 1 MB before it forms the outbound event. The limit applies to each column separately. For example, if a row has five columns and the first three column values are each larger than 1 MB, CES truncates each of those three values to 1 MB, forms the outbound event, splits the event into message chunks according to `@max_message_size_kb`, and sends each chunk to the destination.

          Column data truncation is unconditional. CES doesn't log errors or warnings when this truncation occurs. Configure the [max text repl size](../../../database-engine/configure-windows/configure-the-max-text-repl-size-server-configuration-option.md) server configuration option to allow more than 65,536 bytes to be written to LOB columns when CES is enabled.
      - question: Does a table need a primary key to use CES?
        answer: No. CES doesn't require a primary key on the table.
      - question: How granular are CES events?
        answer: Each row affected by an `INSERT`, `UPDATE`, or `DELETE` operation is streamed as a separate event.
      - question: If a transaction modifies multiple rows, are they streamed as separate events or as one?
        answer: Each affected row is streamed as a separate event. Each event includes transaction metadata, so changes from the same transaction can be logically grouped on the destination.

  - name: Integration and configuration
    questions:
      - question: How does CES work in a high availability (HA) configuration? Can I use a DNN connection string?
        answer: CES only streams from the primary replica. In the event of a failover, CES must be reconfigured on the new primary. You can connect to the primary replica using any method supported by SQL Server, including a distributed network name (DNN). 
      - question: Which protocols does CES use to communicate with the destination?
        answer: CES supports AMQP and Kafka protocols for Azure Event Hubs. HTTPS isn't supported. If you use the AMQP protocol, set the Azure Event Hubs [Minimum TLS version](/azure/event-hubs/transport-layer-security-enforce-minimum-version) configuration option to 1.2. CES doesn't support AMQP when this option is set to 1.3.
      - question: Can CES stream directly to Kafka?
        answer: No. CES currently supports only Azure Event Hubs (AMQP and Kafka protocol) as the destination.
      - question: Does CES support Microsoft Entra managed identity for authentication with Azure Event Hubs?
        answer: Yes. CES supports Microsoft Entra authentication in Azure SQL Database, Azure SQL Managed Instance and SQL Server 2025 starting with Cumulative Update 3 (CU3) for instances [enabled by Azure Arc](../../../sql-server/azure-arc/connect.md) or running on an [Azure VM](/azure/azure-sql/virtual-machines/windows/sql-server-on-azure-vm-iaas-what-is-overview).

  - name: Functionality and features
    questions:
      - question: Can CES be enabled on a database that already uses transactional replication?
        answer: No. CES can't be enabled on a database configured with transactional replication.
      - question: Can CES run alongside change data capture (CDC)?
        answer: No. CES can't be enabled on a database enabled with CDC.
      - question: Can CES be used with Fabric Mirrored Databases for SQL Server?
        answer: No. CES isn't supported with [Fabric Mirrored Databases for SQL Server](/fabric/database/mirrored-database/sql-server).
      - question: What happens if CES is disabled? Does it resume from where it left off when reenabled?
        answer: |
          If CES is disabled, it can't be resumed. You must manually reconfigure CES to continue streaming events. Data changes made while CES was disabled aren't captured. Only changes made after CES is restarted are streamed.
          CES guarantees *at least once* delivery. If CES fails to deliver a message, it continues to retry until the message is successfully delivered, or CES is disabled. If CES isn't disabled, failed deliveries prevent log truncation until the error condition is resolved, and the message is successfully delivered. If CES is disabled, log truncation resumes, and events that occur during the downtime aren't captured.

      - question: What happens if I do a restore (or another type of migration) of a database that was configured for CES?
        answer: After restore (or other types of database migration), CES is disabled and stopped for the database. You have to reconfigure it to start streaming again.
      - question: Can CES do an initial snapshot ("seed") of existing data in tables when CES is enabled?
        answer: No. CES currently only streams changes to data that happen after CES is enabled. It doesn't stream data in a table that exists before CES is enabled.
      - question: How can I monitor CES activity (for example, events processed, errors)?
        answer: |
          Use the following system views to monitor CES activity:
          - [sys.dm_change_feed_errors](../../system-dynamic-management-objects/sys-dm-change-feed-errors.md) for event delivery errors. 
          - [sys.dm_change_feed_log_scan_sessions](../../system-dynamic-management-objects/sys-dm-change-feed-log-scan-sessions.md) for log scan activity.
          
          In SQL Server 2025, you can create a CES-related XEvent session and get more details on CES activity and potential errors.
          
          ```sql
            CREATE EVENT SESSION ChangeEventStreaming
            ON SERVER
            ADD EVENT sqlserver.synapse_link_error
            (
            )
            ADD TARGET package0.event_file 
            (SET filename=N'C:\temp\YourSession_Target1.xel');
            GO
          ```

          When configuring CES XEvent monitoring for Azure SQL Managed Instance, store your `.xel` file in an Azure Blob Storage container. You can observe Change Event Streaming trace information in Azure SQL Managed Instance by observing `synapse_link_error` events: 
          
          ```sql
          CREATE CREDENTIAL
          [your blob container URL here] --make sure to not leave / at the end
          WITH IDENTITY = 'MANAGED IDENTITY';
          
          DROP EVENT SESSION ChangeEventStreaming ON SERVER
          CREATE EVENT SESSION ChangeEventStreaming
          ON SERVER
          ADD EVENT sqlserver.synapse_link_error
          (
          )
          ADD TARGET package0.event_file
          (
              SET filename = N'https://[your container URL]/cessession.xel'
            
          );
          ```

          
          XEvent debugging isn't supported for troubleshooting CES in Azure SQL Database.

          You can monitor Azure Event Hubs and use associated metrics and logs.
      - question: How long are CES errors retained in sys.dm_change_feed_errors?
        answer: The [sys.dm_change_feed_errors](../../system-dynamic-management-objects/sys-dm-change-feed-errors.md) error list auto-rotates. Newer errors push out earlier entries. Errors that stop occurring aren't removed from the list immediately; they remain until newer errors push them out or the list clears completely.
      - question: Is there a limit to how many tables can be configured for streaming?
        answer: Yes. CES supports up to 4,096 stream groups, with up to 40,000 tables per group.
